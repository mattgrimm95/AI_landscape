# AI Landscape — code index for LLMs (and humans)

Autogenerated from module + function docstrings by
`scripts/build_llm_index.py`. Rerun after touching public
module / function / class signatures.

_Last generated: 2026-05-26_

## Reading order

1. **Pipeline core** — `ailandscape/pipeline.py`, `corpus.py`,
   `scraper.py`, `ner.py`, `reconcile.py`, `relations.py`. The data
   flow from feeds to graph.
2. **Storage** — `storage_kg.py`, `storage_ner.py`. SQLite glue.
3. **Shared gates** — `ai_terms.py`. The AI relevance lexicon
   used by SBIR / enrich / scrape filters.
4. **Surface layers** — `server.py` (FastAPI), `cli.py` (CLI),
   `web/` (Cytoscape.js frontend, not indexed here).
5. **LLM integration** — `synthesis.py`, `claude_cli.py`,
   `synthesis_cache.py`. Two transports + a sidecar cache.
6. **Reporting + tours** — `report.py`, `briefing.py`, `trends.py`,
   `tours.py`, `review.py`, `capabilities.py`.
7. **Data sources beyond RSS** — `sbir.py`, `jbooks.py`,
   `feeds.py`, `feed_discovery.py`.
8. **Tests** — `tests/test_*.py`. One file per module.

---

# Package: `ailandscape/`

## `ailandscape/__init__.py`

> AI national-security landscape knowledge-graph pipeline.

*(no public functions or classes)*


## `ailandscape/acronyms.py`

> Apposition-based acronym ↔ expansion extraction.

**Functions**
- `extract_pairs(text)` — Extract ``(acronym, expansion)`` candidate pairs from one document.
- `aggregate(per_doc_pairs)` — Aggregate per-document pairs into corroborated suggestions.


## `ailandscape/ai_terms.py`

> Shared AI-relevance lexicon — the single bar for "is this an AI story?".

**Functions**
- `is_ai_relevant(text)` — True if ``text`` contains an AI / ML / autonomy term.
- `ai_terms_in(text)` — Distinct AI term hits in ``text``, for diagnostic logging.


## `ailandscape/briefing.py`

> A generated briefing of the AI national-security landscape.

**Functions**
- `build_briefing(documents, kg_store, days=7, now=None, subfield_concepts=None)` — Compute the briefing digest from the corpus documents and the graph.
- `render_briefing(data)` — Format the briefing dict into a human-readable report string.


## `ailandscape/capabilities.py`

> Capability subfield index for the AI knowledge graph.

**Functions**
- `build_capabilities(nodes, edges, top_orgs=5, top_concepts=8)` — Build one card per AI subfield.
- `subfield_concept_names(subfield_id)` — Return the canonical concept names belonging to a subfield, or [].


## `ailandscape/claude_cli.py`

> Claude Code CLI transport for headless synthesis.

**Functions**
- `find_cli()` — Return the absolute path to the `claude` executable, or None.
- `is_available()` — True if the Claude Code CLI can be invoked from this process.
- `summarize(prompt, model=None, timeout=DEFAULT_TIMEOUT)` — Send `prompt` to the Claude Code CLI in one-shot mode, return text.

**Classes**
- `class ClaudeCliError` — Raised when the CLI is missing, errors out, or returns no text.


## `ailandscape/cli.py`

> Command-line interface for the AI landscape pipeline.

**Functions**
- `cmd_run(args)` — *(no docstring)*
- `cmd_jbooks(args)` — Pull AI-related R&D items from DoD J-Books into the corpus, rebuild.
- `cmd_sbir(args)` — Pull AI-related SBIR/STTR awards into the corpus, then rebuild.
- `cmd_backfill(_args)` — Re-fetch corpus documents that stored only a short feed summary.
- `cmd_rebuild(args)` — *(no docstring)*
- `cmd_demo(args)` — *(no docstring)*
- `cmd_stats(_args)` — *(no docstring)*
- `cmd_overview(args)` — *(no docstring)*
- `cmd_briefing(args)` — Print a generated briefing of the AI national-security landscape.
- `cmd_digest(args)` — Compose the daily digest; with --preview, print it; otherwise send it to the recipients in data/email_recipients.txt via configured SMTP.
- `cmd_review(_args)` — Audit data quality; accumulate findings in review.json.
- `cmd_trends(_args)` — Print temporal trends: document volume and new / active entities.
- `cmd_visualize(args)` — *(no docstring)*
- `cmd_correct(args)` — *(no docstring)*
- `cmd_correct_from_review(args)` — Walk `review.json` suggestions and bulk-apply approved ones.
- `cmd_history(args)` — Print the ingest-history log as a readable table.
- `cmd_audit_corpus_ai(args)` — Scan the corpus for documents that don't pass the AI relevance bar.
- `cmd_synthesize_daily(args)` — Generate today's hype + briefing-narrative sidecar snapshot.
- `cmd_enrich(args)` — Run an entity-enrichment plan: fetch the listed articles + optional synthesis, append them to the corpus, then rebuild so the new entities propagate into the NER log and the knowledge graph.
- `cmd_discover_feeds(args)` — Probe candidate RSS URLs for AI/national-security organizations.
- `cmd_serve(args)` — *(no docstring)*
- `cmd_snapshot(_args)` — *(no docstring)*
- `cmd_reading(args)` — Show Claude-reading coverage of the corpus.
- `cmd_reset(args)` — *(no docstring)*
- `build_parser()` — *(no docstring)*
- `main(argv=None)` — *(no docstring)*


## `ailandscape/config.py`

> Filesystem paths and shared constants.

**Functions**
- `ensure_dirs()` — Create the data, snapshot, and corpus directories if they are missing.


## `ailandscape/corpus.py`

> The corpus: the version-controlled source of truth.

**Functions**
- `load(path)` — Return every document record in the corpus, in append order.
- `hashes(path)` — Return the set of content hashes already present in the corpus.
- `append(path, document)` — Append one document record as a JSON line.
- `save(path, documents)` — Overwrite the corpus with `documents`, atomically.
- `count(path)` — *(no docstring)*
- `mark_read(path, content_hashes, when_iso)` — Mark the documents identified by `content_hashes` as Claude-read.
- `invalidate_freshness(path)` — Flip `claude_read_fresh` to False on every document.
- `reading_stats(path)` — Return a small dict summarizing Claude read coverage of the corpus.
- `document_text(doc)` — The text NER and relation extraction operate on: title + body.
- `published_date(doc)` — Best-effort parse of a document's published date to 'YYYY-MM-DD'.
- `published_date_status(doc)` — Like `published_date`, but also reports why a parse returned ''.


## `ailandscape/emailer.py`

> Private email distribution of the daily landscape digest.

**Functions**
- `load_recipients(path)` — Read recipient email addresses from `path`, ignoring blanks/comments.
- `smtp_config()` — SMTP settings from the environment.
- `build_digest(documents, kg_store, days=7)` — Compose the digest body text from the briefing and POC leads.
- `send_digest(recipients, body, subject, smtp, sender_fn=None)` — Send a composed digest. `sender_fn` is injectable for tests.
- `daily_digest(documents, kg_store, recipients_path, days=7, sender_fn=None)` — Build and send the daily digest, opt-in.

**Classes**
- `class EmailError` — Raised when the digest cannot be sent.


## `ailandscape/enrich.py`

> Entity enrichment — pull targeted web content into the corpus.

**Functions**
- `plan_ai_signal(plan)` — Return the AI-relevance signal for an enrichment plan as a dict.
- `add_article(corpus_path, url, title, source, published='', metadata=None, prefetched_html=None, log=None)` — Fetch a URL, extract its main text, append to the corpus.
- `add_synthesis(corpus_path, entity, body, title=None, source='Claude synthesis', log=None)` — Append a Claude-written entity overview to the corpus.
- `enrich_from_plan(corpus_path, plan, log=None, allow_non_ai=False)` — Execute an enrichment plan: fetch the articles + append the synthesis.


## `ailandscape/feed_discovery.py`

> Discover new RSS/Atom feeds for the AI national-security landscape.

**Functions**
- `verify_feed(url)` — Verify that `url` returns a parseable RSS/Atom feed.
- `discover_candidates(seeds)` — Probe standard RSS URL templates for each seed.
- `health_check_existing(feeds)` — Verify every URL in `feeds` — caller-supplied (e.g. feeds.FEEDS).


## `ailandscape/feeds.py`

> Source feed definitions for the AI / national-security landscape.

*(no public functions or classes)*


## `ailandscape/gazetteer.py`

> Curated national-security gazetteer for high-precision entity matching.

**Functions**
- `subfield_for_concept(canonical_name)` — Return the subfield id a concept belongs to, or None.


## `ailandscape/jbooks.py`

> DoD budget Justification Books (J-Books) — a non-RSS data source.

**Functions**
- `is_ai_related(text)` — True if J-book text mentions AI or ML.
- `is_rdte(text)` — True if the document reads as R&D / RDT&E content.
- `extract_program_elements(text)` — Split a J-book's text into per-program-element chunks.
- `pe_to_article(chunk, source_url, fiscal_year, agency)` — Convert one program-element chunk into a scraper-style article dict.
- `ai_articles(pdf_text, source_url, fiscal_year, agency)` — Return AI-related R&D program-element articles from a J-book's text.
- `find_pdf_links(html, base_url)` — Heuristically pull PDF links out of a budget-materials index page.
- `fetch_jbook_articles(index_url, fiscal_year, agency, max_pdfs=DEFAULT_MAX_PDFS, log=None)` — Crawl a budget-materials index page; return AI-related R&D articles.

**Classes**
- `class JBookError` — Raised when J-books cannot be fetched or parsed.


## `ailandscape/ner.py`

> Step 2 of the flow: named entity recognition.

**Functions**
- `default_backend()` — Return the configured default NER backend ('rule' or 'spacy').
- `extract(text, backend=None)` — Extract entities from `text`.


## `ailandscape/pipeline.py`

> Orchestrates the full app_plan flow.

**Functions**
- `make_record(article)` — Turn a freshly scraped article into a corpus document record.
- `fetch_all(feeds, log=None)` — Step 1: fetch and parse every feed, tolerating per-feed failures.
- `scrape_into_corpus(feeds, corpus_path, log=None)` — Step 1: fetch feeds, then for each previously unseen article fetch its page, extract the clean main text, and append it to the corpus.
- `scrape_sbir_into_corpus(sbir_queries, corpus_path, log=None)` — Step 1 (non-RSS): pull AI-related SBIR/STTR awards into the corpus.
- `backfill_corpus_text(corpus_path, log=None)` — Re-fetch the page text of corpus documents that stored only a short summary — articles whose pages failed to extract at scrape time and fell back to the feed's teaser.
- `scrape_jbooks_into_corpus(jbook_sources, corpus_path, log=None)` — Step 1 (non-RSS): pull AI-related R&D items from DoD J-Books.
- `rebuild(corpus_path, ner_log, kg_store, ner_backend=None, corrections=None, log=None)` — Regenerate the NER output log and knowledge graph from the corpus (steps 2-5).
- `generate_daily_syntheses(documents, kg_store, log=None, force=False)` — Generate today's hype + briefing-narrative snapshot, if a key is set.
- `run(feeds, corpus_path, ner_log, kg_store, sbir_queries=None, jbook_sources=None, ner_backend=None, corrections=None, log=None)` — Run the entire flow: scrape into the corpus, then rebuild everything.


## `ailandscape/reconcile.py`

> Step 4 of the flow: filter / de-duplicate / reconcile / relationship links.

**Functions**
- `normalize(text)` — Normalize an entity surface form into a dedup/alias key.
- `load_corrections(path)` — Load a manual-corrections file.
- `reconcile(documents, ner_log, kg_store, corrections=None, log=None)` — Build the knowledge graph from the corpus documents and NER log.


## `ailandscape/relations.py`

> Typed relationship extraction (part of step 4).

**Functions**
- `extract_relations(text, entities)` — Extract typed relationships from one document.


## `ailandscape/report.py`

> A statistical overview of the pipeline's data.

**Functions**
- `build_overview(documents, ner_log, kg_store, run_history_path=None)` — Compute overview metrics from the corpus, NER log, and graph.
- `diff_runs(run_history_path)` — Compute KPI deltas between the two most recent runs.
- `render_overview(data)` — Format the overview dict into a human-readable report string.
- `render_diff(diff)` — Format the run-to-run KPI deltas into a readable diff report.


## `ailandscape/review.py`

> Corpus / knowledge-graph quality review.

**Functions**
- `build_review(documents, kg_store)` — Audit the graph and return quality findings worth acting on.
- `render_review(data)` — Format the review findings into a human-readable report string.
- `save_review(data, path)` — Merge this review's findings into the accumulating review store.


## `ailandscape/sbir.py`

> SBIR / STTR award ingestion — a non-RSS data source (part of step 1).

**Functions**
- `is_ai_related(award)` — True if an award's title / abstract / keywords mention AI or ML.
- `fetch_awards(agency='DOD', year=None, max_records=200)` — Fetch awards for one agency (optionally one year) from the API.
- `award_to_article(award)` — Convert one SBIR award record into a scraper-style article dict.
- `ai_articles(awards)` — Filter `awards` to AI-related ones and convert them to article dicts.
- `load_fixture(path)` — Load awards from a local JSON file (used for deterministic tests).

**Classes**
- `class SBIRError` — Raised when the SBIR API cannot be reached or returns an error.


## `ailandscape/scraper.py`

> Step 1 of the flow: scrape articles.

**Functions**
- `html_to_text(html)` — Strip HTML tags and collapse whitespace into a plain-text string.
- `parse_feed(raw, source_name)` — Parse RSS/Atom feed content into a list of article dicts.
- `content_hash(article)` — Stable id for an article, derived from its URL and title.
- `fetch_feed(feed)` — Fetch and parse a live feed. `feed` is a dict with 'name' and 'url'.
- `extract_text_from_html(html, fallback='')` — Extract an article's main text from page HTML using trafilatura.
- `extract_article(url, fallback='')` — Fetch an article page and return its clean main text.
- `scrape_fixture(path, source_name)` — Parse a feed from a local file (used for deterministic tests).

**Classes**
- `class FeedError` — Raised when a feed cannot be fetched.


## `ailandscape/server.py`

> FastAPI backend for the knowledge-graph web app.

**Functions**
- `api_graph(focus=None, type=None, min_mentions=0, max_nodes=Query(90, ge=1, le=400), min_weight=Query(2, ge=1), relations_only=False, min_confidence=Query(0.0, ge=0.0, le=1.0), min_strength=Query(0.0, ge=0.0, le=1.0), src_type=None, dst_type=None)` — Return a comprehensible subgraph as {nodes, edges}.
- `api_search(q, limit=Query(20, ge=1, le=100), since=None, relation=None)` — Search entities (by canonical name or any alias) and documents.
- `api_path(from_=Query(..., alias='from'), to=Query(...))` — Find how two entities are connected: the shortest path between them.
- `api_node(node_id)` — Return a node and its neighbors, ranked by edge weight.
- `api_node_documents(node_id, limit=Query(50, ge=1, le=200))` — Return the source articles a node appears in, most recent first.
- `api_tours()` — Return the curated story tours for the sidebar / guide.
- `api_types()` — Entity types and how many nodes each has (for the filter UI).
- `api_overview()` — The statistical overview as structured JSON.
- `api_history(limit=Query(60, ge=1, le=365))` — Pipeline ingest history -- one record per `ailandscape run` invocation.
- `api_trends()` — Temporal signals — document volume by month, new and active entities.
- `api_document(content_hash)` — Return one corpus document's full text + metadata for in-app reading.
- `api_document_mark_read(payload)` — Stamp one document as Claude-read (counter += 1, fresh = True).
- `api_recent(since=None, days=Query(7, ge=1, le=90))` — What's new since `since` (YYYY-MM-DD) — articles + entities.
- `api_pulse(days=Query(7, ge=1, le=30))` — A short always-visible "what's happening" strip.
- `api_spikes(limit=Query(20, ge=1, le=100))` — Entities whose recent mention rate is sharply above their baseline.
- `api_capabilities()` — The AI subfield map — one card per subfield with leading concepts + top organizations + mention totals.
- `api_trajectory(months=Query(12, ge=2, le=36))` — Corpus-wide month-by-month trajectory.
- `api_node_adjacent(node_id, limit=Query(6, ge=1, le=20))` — Entities two hops away from this one — the "you may not know" list.
- `api_briefing(days=Query(7, ge=1, le=90), subfield=None)` — A generated briefing of the landscape as structured JSON.
- `api_hype(refresh=Query(0, ge=0, le=1))` — Serve the daily hype-read synthesis from the sidecar snapshot.
- `api_briefing_narrative(refresh=Query(0, ge=0, le=1))` — Serve the daily briefing-narrative synthesis from the sidecar snapshot.
- `api_correct(correction)` — Record a manual correction, then re-reconcile so it takes effect.

**Classes**
- `class MarkRead` — *(no docstring)*
- `class Correction` — *(no docstring)*


## `ailandscape/storage_kg.py`

> Step 5 of the flow: the knowledge graph database.

**Classes**
- `class KnowledgeGraphStore` — Store for knowledge-graph nodes, aliases, and relationship edges.
  - `close(self)` — *(no docstring)*
  - `clear(self)` — Remove all graph data.
  - `insert_node(self, canonical_name, node_type, first_seen='', last_seen='', mention_count=0, document_count=0, metadata=None)` — *(no docstring)*
  - `insert_alias(self, node_id, alias)` — *(no docstring)*
  - `insert_edge(self, src_id, dst_id, relation, weight=1, metadata=None)` — *(no docstring)*
  - `insert_node_documents(self, node_id, content_hashes)` — Record which corpus documents a node was mentioned in.
  - `documents_for_node(self, node_id)` — Return the content hashes of the documents a node appears in.
  - `commit(self)` — *(no docstring)*
  - `node_by_alias(self, alias)` — *(no docstring)*
  - `nodes(self)` — *(no docstring)*
  - `aliases(self)` — *(no docstring)*
  - `edges(self)` — *(no docstring)*
  - `top_nodes(self, limit=10)` — *(no docstring)*
  - `count_nodes(self)` — *(no docstring)*
  - `count_edges(self)` — *(no docstring)*


## `ailandscape/storage_ner.py`

> Step 3 of the flow: the NER output log.

**Classes**
- `class NEROutputLog` — Derived store for raw NER output, keyed to corpus documents.
  - `close(self)` — *(no docstring)*
  - `clear(self)` — Remove all rows so the log can be rebuilt from the corpus.
  - `add_entities(self, content_hash, entities)` — Append raw entity records for one corpus document.
  - `entities_for(self, content_hash)` — *(no docstring)*
  - `all_entities(self)` — *(no docstring)*
  - `count_entities(self)` — *(no docstring)*


## `ailandscape/synthesis.py`

> Optional LLM narrative synthesis.

**Functions**
- `transport()` — Return the name of the preferred synthesis transport, or None.
- `is_configured()` — True if some synthesis transport is available on this machine.
- `summarize_briefing(briefing_data, max_tokens=700)` — Return an LLM-written narrative summary of a briefing dict.
- `summarize_hype(documents, sbir_funding=None, max_tokens=400)` — Return an exciting, 30-second-read hype summary of recent AI news.

**Classes**
- `class SynthesisError` — Raised when narrative synthesis is unavailable or the call fails.


## `ailandscape/synthesis_cache.py`

> Daily snapshot store for LLM-written syntheses (hype + briefing narrative).

**Functions**
- `snapshot_path(date=None)` — Path the snapshot for `date` would live at (no I/O performed).
- `latest_snapshot_path()` — Return the path of the most recent snapshot, or None if there isn't one.
- `load_snapshot(path)` — Read and parse a snapshot file.
- `latest_snapshot()` — Load the most recent snapshot.
- `save_snapshot(snapshot, date=None)` — Atomically write a snapshot for `date` (defaults to today UTC).
- `empty_section()` — A blank section in the snapshot schema — for failed / never-tried sub-runs.
- `make_snapshot(corpus_mtime=None, corpus_documents=0)` — Initialise a snapshot dict for today with both sections blank.
- `set_section(snapshot, name, text='', available=True, error='', window_days=0, documents_used=0)` — Write one section into an in-progress snapshot.
- `age_seconds(snapshot, now=None)` — How many seconds old this snapshot is, or None if it has no timestamp.
- `is_stale(snapshot, max_age_hours=STALE_AFTER_HOURS, now=None)` — True if the snapshot is older than `max_age_hours`.


## `ailandscape/tours.py`

> Curated story tours through the knowledge graph.

**Functions**
- `build_tour_index()` — Return the curated tours as a plain JSON-safe list for the API.
- `get_tour(tour_id)` — Return one tour by id, or None.


## `ailandscape/trends.py`

> Temporal signals over the knowledge graph.

**Functions**
- `build_trends(documents, kg_store)` — Compute temporal signals from the corpus documents and the graph.
- `build_recent(documents, kg_store, since=None, days=7, limit=15)` — Return the slice of the landscape that's new since `since` (or `days`).
- `build_spikes(documents, kg_store, recent_days=30, min_recent=5, spike_ratio=3.0, min_active_days=60, limit=20)` — Entities whose recent mention rate is sharply above their baseline.
- `build_trajectory(documents, kg_store, months=12)` — Corpus-wide month-by-month trajectory.
- `render_trends(data)` — Format the trends dict into a human-readable report string.


## `ailandscape/visualize.py`

> Step 7 of the flow: an interactive visualization of the knowledge graph.

**Functions**
- `select_subgraph(nodes, edges, focus=None, node_type=None, min_mentions=0, max_nodes=70, min_weight=3, relations_only=False, min_confidence=0.0, min_strength=0.0, src_type=None, dst_type=None)` — Pick a comprehensible subgraph to render.
- `find_path(nodes, edges, src_id, dst_id)` — Shortest path between two node ids, as a list of (from, to, edge) steps.
- `render(nodes, edges, output_path, title='AI Landscape Knowledge Graph')` — Render a subgraph to a self-contained interactive HTML file.



# Scripts: `scripts/`

## `scripts/build_llm_index.py`

> Autogenerate LLM_INDEX.md from module docstrings + public function signatures.

**Functions**
- `build()` — Generate the index markdown string.
- `main(argv=None)` — *(no docstring)*



# Tests: `tests/`

## `tests/__init__.py`

> *(no module docstring — consider adding one)*

*(no public functions or classes)*


## `tests/test_acronyms.py`

> Acronym ↔ expansion extraction tests.

**Classes**
- `class ExtractPairsTest` — *(no docstring)*
  - `test_expansion_paren_pattern(self)` — *(no docstring)*
  - `test_acronym_paren_pattern(self)` — *(no docstring)*
  - `test_lowercase_expansion_with_skipwords(self)` — *(no docstring)*
  - `test_skipwords_excluded_from_initials(self)` — *(no docstring)*
  - `test_false_positive_initials_mismatch_rejected(self)` — *(no docstring)*
  - `test_unrelated_parenthetical_rejected(self)` — *(no docstring)*
  - `test_empty_text(self)` — *(no docstring)*
- `class AggregateTest` — *(no docstring)*
  - `test_gated_by_min_doc_freq(self)` — *(no docstring)*
  - `test_corroborated_across_two_docs_surfaces(self)` — *(no docstring)*
  - `test_conflicting_expansions_kept_separate(self)` — *(no docstring)*
  - `test_duplicate_pairs_in_one_doc_count_once(self)` — *(no docstring)*


## `tests/test_ai_terms.py`

> Tests for the shared AI-relevance lexicon.

**Classes**
- `class AiTermsCorePatternsTest` — The original SBIR terms must still match (no regression).
  - `test_core_ai_terms(self)` — *(no docstring)*
  - `test_acronyms_case_sensitive(self)` — *(no docstring)*
- `class AiTermsDefenseOverlapTest` — The expanded defense-AI overlap vocabulary.
  - `test_jadc2_acronym(self)` — *(no docstring)*
  - `test_mum_t_and_c_uas(self)` — *(no docstring)*
  - `test_named_ai_platforms(self)` — *(no docstring)*
  - `test_collaborative_combat_aircraft_and_loitering(self)` — *(no docstring)*
  - `test_sensor_fusion_phrases(self)` — *(no docstring)*
  - `test_frontier_and_agentic(self)` — *(no docstring)*
  - `test_edge_inference(self)` — *(no docstring)*
- `class AiTermsIntentionalGapsTest` — Terms deliberately NOT in the lexicon to avoid over-matching.
  - `test_plain_electronic_warfare_does_not_match(self)` — *(no docstring)*
  - `test_plain_drone_does_not_match(self)` — *(no docstring)*
  - `test_real_non_ai_titles_rejected(self)` — *(no docstring)*
- `class AiTermsHelpersTest` — *(no docstring)*
  - `test_is_ai_relevant_handles_none_and_empty(self)` — *(no docstring)*
  - `test_ai_terms_in_returns_distinct_hits(self)` — *(no docstring)*
  - `test_sbir_reexports_same_objects(self)` — *(no docstring)*


## `tests/test_briefing.py`

> *(no module docstring — consider adding one)*

**Classes**
- `class BriefingTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_briefing_structure_and_window(self)` — *(no docstring)*
  - `test_contract_awards_and_trending_topics(self)` — *(no docstring)*
  - `test_sbir_funding_totals(self)` — *(no docstring)*
  - `test_render_briefing_produces_text(self)` — *(no docstring)*
- `class SubfieldBriefingTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_subfield_scopes_entities_and_documents(self)` — *(no docstring)*


## `tests/test_build_llm_index.py`

> Smoke test for scripts/build_llm_index.py.

**Classes**
- `class BuildLlmIndexTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `test_build_returns_markdown(self)` — *(no docstring)*
  - `test_first_sentence_trims_at_sentence_boundary(self)` — *(no docstring)*
  - `test_first_sentence_keeps_us_inside_a_sentence(self)` — *(no docstring)*
  - `test_run_is_idempotent(self)` — *(no docstring)*


## `tests/test_capabilities.py`

> Capability subfield index tests.

**Classes**
- `class BuildCapabilitiesTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_returns_card_per_subfield(self)` — *(no docstring)*
  - `test_concepts_resolved_from_live_nodes(self)` — *(no docstring)*
  - `test_top_organizations_ranked_by_cooccurrence(self)` — *(no docstring)*
  - `test_subfield_concept_names_lookup(self)` — *(no docstring)*
- `class GazetteerSubfieldTest` — *(no docstring)*
  - `test_subfields_cover_no_overlap(self)` — *(no docstring)*
  - `test_subfield_for_concept_lookup(self)` — *(no docstring)*


## `tests/test_cli_correct_from_review.py`

> Bulk-apply suggestions from review.json into corrections.json.

**Classes**
- `class CorrectFromReviewTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_missing_review_file_returns_error(self)` — *(no docstring)*
  - `test_nothing_to_apply_when_no_flag_chosen(self)` — *(no docstring)*
  - `test_applies_merges_with_yes_flag(self)` — *(no docstring)*
  - `test_applies_ignores_with_yes_flag(self)` — *(no docstring)*
  - `test_existing_corrections_are_preserved(self)` — *(no docstring)*
  - `test_applies_corroborated_acronyms(self)` — *(no docstring)*
  - `test_skips_existing_merge_keys(self)` — *(no docstring)*


## `tests/test_corpus.py`

> *(no module docstring — consider adding one)*

**Classes**
- `class CorpusTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `test_load_missing_file_returns_empty(self)` — *(no docstring)*
  - `test_append_and_load_roundtrip(self)` — *(no docstring)*
  - `test_append_projects_to_fixed_fields(self)` — *(no docstring)*
  - `test_hashes(self)` — *(no docstring)*
  - `test_count(self)` — *(no docstring)*
  - `test_save_overwrites_corpus(self)` — *(no docstring)*
  - `test_save_projects_to_fixed_fields(self)` — *(no docstring)*
  - `test_metadata_round_trips(self)` — *(no docstring)*
  - `test_load_defaults_metadata_for_older_lines(self)` — *(no docstring)*
  - `test_new_documents_default_to_unread(self)` — *(no docstring)*
  - `test_mark_read_increments_and_sets_fresh(self)` — *(no docstring)*
  - `test_mark_read_accumulates_across_calls(self)` — *(no docstring)*
  - `test_invalidate_freshness_preserves_count(self)` — *(no docstring)*
  - `test_reading_stats_buckets(self)` — *(no docstring)*
  - `test_load_defaults_reading_fields_for_older_lines(self)` — *(no docstring)*
  - `test_published_date_parses_formats(self)` — *(no docstring)*
  - `test_published_date_status_distinguishes_missing_vs_unparseable(self)` — *(no docstring)*


## `tests/test_corpus_archive.py`

> Tests for the corpus archive (audit-corpus-ai --prune / --reinstate).

**Classes**
- `class CorpusArchiveTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_prune_moves_to_archive(self)` — *(no docstring)*
  - `test_protected_docs_never_dropped(self)` — *(no docstring)*
  - `test_reinstate_recovers_now_passing_docs(self)` — *(no docstring)*
  - `test_reinstate_dedupes_against_active_corpus(self)` — *(no docstring)*
  - `test_audit_only_does_not_touch_files(self)` — *(no docstring)*
- `class HistoryCommandTest` — The `ailandscape history` CLI surfaces the per-run JSONL nicely.
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_history_empty(self)` — *(no docstring)*
  - `test_history_reads_seeded_records(self)` — *(no docstring)*


## `tests/test_emailer.py`

> *(no module docstring — consider adding one)*

**Classes**
- `class EmailerTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_load_recipients_skips_comments_and_blanks(self)` — *(no docstring)*
  - `test_daily_digest_no_op_without_recipients(self)` — *(no docstring)*
  - `test_daily_digest_no_op_without_smtp(self)` — *(no docstring)*
  - `test_daily_digest_sends_with_mocked_smtp(self)` — *(no docstring)*


## `tests/test_enrich.py`

> Unit tests for the entity-enrichment module.

**Classes**
- `class AiRelevanceGateTest` — The gate logic in isolation (no corpus I/O).
  - `test_is_ai_relevant_positive_phrase(self)` — *(no docstring)*
  - `test_is_ai_relevant_positive_acronym(self)` — *(no docstring)*
  - `test_is_ai_relevant_negative(self)` — *(no docstring)*
  - `test_is_ai_relevant_empty(self)` — *(no docstring)*
  - `test_lowercase_ai_in_word_does_not_match(self)` — *(no docstring)*
  - `test_plan_signal_synthesis_match(self)` — *(no docstring)*
  - `test_plan_signal_article_match(self)` — *(no docstring)*
  - `test_plan_signal_no_match(self)` — *(no docstring)*
- `class EnrichFromPlanGateTest` — The gate's behaviour when enrich_from_plan actually runs.
  - `setUp(self)` — *(no docstring)*
  - `test_ai_plan_lands(self)` — *(no docstring)*
  - `test_synthesis_only_signal_admits_plan(self)` — *(no docstring)*
  - `test_non_ai_plan_is_rejected_by_default(self)` — *(no docstring)*
  - `test_allow_non_ai_bypass(self)` — *(no docstring)*
- `class RealEnrichmentPlansTest` — Regression: the two enrichment plans on disk must still pass.
  - `test_palantir_plan_passes(self)` — *(no docstring)*
  - `test_b21_plan_passes(self)` — *(no docstring)*


## `tests/test_golden_snapshot.py`

> Golden-snapshot regression guard for the rebuild pipeline.

**Classes**
- `class GoldenSnapshotTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `test_rebuild_matches_golden_counts(self)` — *(no docstring)*


## `tests/test_jbooks.py`

> *(no module docstring — consider adding one)*

**Classes**
- `class JBooksTest` — *(no docstring)*
  - `test_is_ai_related_matches_machine_learning(self)` — *(no docstring)*
  - `test_is_ai_acronym_case_sensitive(self)` — *(no docstring)*
  - `test_is_rdte_requires_marker(self)` — *(no docstring)*
  - `test_extract_program_elements_splits_on_headers(self)` — *(no docstring)*
  - `test_ai_articles_keeps_only_ai_program_elements(self)` — *(no docstring)*
  - `test_non_rdte_document_yields_no_articles(self)` — *(no docstring)*
  - `test_find_pdf_links_resolves_relative_urls(self)` — *(no docstring)*
  - `test_fetch_jbook_articles_raises_on_index_failure(self)` — *(no docstring)*


## `tests/test_ner.py`

> *(no module docstring — consider adding one)*

**Functions**
- `texts(entities)` — *(no docstring)*

**Classes**
- `class NerTest` — *(no docstring)*
  - `test_gazetteer_entities(self)` — *(no docstring)*
  - `test_gazetteer_canonicalization(self)` — *(no docstring)*
  - `test_ai_capability_concepts(self)` — *(no docstring)*
  - `test_lowercase_single_word_not_an_entity(self)` — *(no docstring)*
  - `test_proper_noun_detection(self)` — *(no docstring)*
  - `test_person_title_is_stripped(self)` — *(no docstring)*
  - `test_chained_person_titles_are_stripped(self)` — *(no docstring)*
  - `test_multi_connector_phrases_are_kept_together(self)` — *(no docstring)*
  - `test_offsets_point_at_surface_text(self)` — *(no docstring)*
  - `test_empty_text(self)` — *(no docstring)*
  - `test_default_backend_is_known(self)` — *(no docstring)*
  - `test_hybrid_backend_combines_gazetteer_and_spacy(self)` — *(no docstring)*


## `tests/test_ner_adversarial.py`

> Adversarial inputs for the NER + reconcile pipeline.

**Classes**
- `class NerAdversarialTest` — *(no docstring)*
  - `assertNoSuspicious(self, text, title='', allow=())` — The final graph must contain no node from this adversarial input.
  - `test_code_block_identifiers_do_not_become_entities(self)` — *(no docstring)*
  - `test_url_slugs_do_not_become_entities(self)` — *(no docstring)*
  - `test_css_class_names_do_not_become_entities(self)` — *(no docstring)*
  - `test_math_notation_does_not_become_entities(self)` — *(no docstring)*
  - `test_emoji_runs_do_not_become_entities(self)` — *(no docstring)*
  - `test_all_caps_headers_yield_no_pseudo_entity(self)` — *(no docstring)*
  - `test_mixed_language_prose_does_not_produce_boilerplate(self)` — *(no docstring)*
  - `test_extreme_phrase_length_is_bounded(self)` — *(no docstring)*
  - `test_url_inside_text_does_not_leak_as_entity(self)` — *(no docstring)*
  - `test_email_inside_text_does_not_leak_as_entity(self)` — *(no docstring)*
  - `test_boilerplate_tokens_dropped_by_reconcile(self)` — *(no docstring)*
  - `test_short_aliases_dropped_by_reconcile(self)` — *(no docstring)*
  - `test_pure_digit_strings_dropped_by_reconcile(self)` — *(no docstring)*
  - `test_real_entity_inside_quotes_survives(self)` — *(no docstring)*


## `tests/test_no_secrets.py`

> Guards against committing secrets or API keys to the repository.

**Classes**
- `class NoSecretsTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `test_no_api_keys_or_private_keys(self)` — *(no docstring)*
  - `test_no_hardcoded_credentials(self)` — *(no docstring)*


## `tests/test_pipeline.py`

> *(no module docstring — consider adding one)*

**Classes**
- `class PipelineTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `test_rebuild_from_corpus(self)` — *(no docstring)*
  - `test_rebuild_is_deterministic(self)` — *(no docstring)*
  - `test_rebuild_into_same_store_does_not_accumulate(self)` — *(no docstring)*
- `class FeedHealthScorecardTest` — `scrape_into_corpus` should now emit a per-feed scorecard.
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_per_feed_stats_track_each_source(self)` — *(no docstring)*
- `class AiRelevanceFilterTest` — Per-feed AI-relevance filter applied during scrape_into_corpus.
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_default_filter_drops_non_ai_articles(self)` — *(no docstring)*
  - `test_ai_only_false_bypasses_filter(self)` — *(no docstring)*
  - `test_feed_default_is_filter_on(self)` — *(no docstring)*
- `class QualityKpiTest` — Pipeline `run` must capture quality KPIs in the run-history record.
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_run_records_quality_kpis(self)` — *(no docstring)*
- `class BackfillTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `test_short_doc_repaired_long_doc_untouched(self)` — *(no docstring)*
  - `test_failed_refetch_leaves_corpus_unchanged(self)` — *(no docstring)*


## `tests/test_reconcile.py`

> *(no module docstring — consider adding one)*

**Classes**
- `class ReconcileTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_normalize_collapses_wording_variants(self)` — *(no docstring)*
  - `test_normalize_strips_leading_article(self)` — *(no docstring)*
  - `test_leading_article_merges_node_and_cleans_display(self)` — *(no docstring)*
  - `test_dedup_and_edges(self)` — *(no docstring)*
  - `test_node_documents_are_persisted(self)` — *(no docstring)*
  - `test_co_occurrence_edge_carries_strength(self)` — *(no docstring)*
  - `test_node_dates_use_published_not_fetched(self)` — *(no docstring)*
  - `test_rebuild_is_idempotent(self)` — *(no docstring)*
  - `test_noise_is_ignored(self)` — *(no docstring)*
  - `test_corrections_merge(self)` — *(no docstring)*
  - `test_wording_variants_merge_node_and_edge(self)` — *(no docstring)*
  - `test_person_coreference_merges_partial_name(self)` — *(no docstring)*
  - `test_typed_edge_stores_confidence_from_weight(self)` — *(no docstring)*
  - `test_typed_edge_stores_evidence(self)` — *(no docstring)*
  - `test_coreference_leaves_ambiguous_surnames_alone(self)` — *(no docstring)*
  - `test_organization_coreference_merges_partial_name(self)` — *(no docstring)*
  - `test_org_coreference_leaves_ambiguous_first_word_alone(self)` — *(no docstring)*
  - `test_single_word_non_gazetteer_single_doc_is_pruned(self)` — *(no docstring)*
  - `test_lowercase_single_word_entity_is_pruned(self)` — *(no docstring)*
  - `test_attribute_extraction_strips_email_boilerplate(self)` — *(no docstring)*
  - `test_gazetteer_entity_kept_even_in_one_document(self)` — *(no docstring)*
  - `test_email_coreference_merges_persons_sharing_address(self)` — *(no docstring)*
  - `test_email_coreference_skips_shared_inbox(self)` — *(no docstring)*


## `tests/test_relations.py`

> *(no module docstring — consider adding one)*

**Classes**
- `class RelationsTest` — *(no docstring)*
  - `test_develops_relation(self)` — *(no docstring)*
  - `test_awards_contract_relation(self)` — *(no docstring)*
  - `test_relation_carries_evidence_snippet(self)` — *(no docstring)*
  - `test_type_constraint_rejects_bad_subject(self)` — *(no docstring)*
  - `test_sentence_break_blocks_relation(self)` — *(no docstring)*
  - `test_distant_entities_are_not_related(self)` — *(no docstring)*
  - `test_entities_without_offsets_yield_nothing(self)` — *(no docstring)*
  - `test_start_char_offsets_are_accepted(self)` — *(no docstring)*
  - `test_passive_voice_flips_direction(self)` — *(no docstring)*
  - `test_passive_develops_flips_direction(self)` — *(no docstring)*
  - `test_located_in_relation(self)` — *(no docstring)*


## `tests/test_report.py`

> *(no module docstring — consider adding one)*

**Classes**
- `class ReportTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_build_overview_funnel_and_breakdowns(self)` — *(no docstring)*
  - `test_overview_flags_quality_issues(self)` — *(no docstring)*
  - `test_render_overview_produces_readable_text(self)` — *(no docstring)*
  - `test_overview_handles_empty_data(self)` — *(no docstring)*
- `class DateQualityTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_unparseable_dates_surface_per_source(self)` — *(no docstring)*
  - `test_render_includes_date_coverage_section(self)` — *(no docstring)*
- `class FeedHealthTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_stale_feeds_are_surfaced(self)` — *(no docstring)*
- `class SignalCoverageTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_documents_without_entities_are_counted(self)` — *(no docstring)*
- `class DiffRunsTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `test_diff_none_with_single_run(self)` — *(no docstring)*
  - `test_diff_computes_deltas_and_percentages(self)` — *(no docstring)*
  - `test_render_diff_highlights_large_changes(self)` — *(no docstring)*


## `tests/test_review.py`

> *(no module docstring — consider adding one)*

**Classes**
- `class ReviewTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_build_review_finds_partial_name_dups(self)` — *(no docstring)*
  - `test_save_review_accumulates_without_overwriting(self)` — *(no docstring)*
  - `test_review_flags_structural_noise(self)` — *(no docstring)*
  - `test_noise_filter_respects_gazetteer(self)` — *(no docstring)*
  - `test_noise_filter_respects_doc_frequency(self)` — *(no docstring)*
  - `test_review_does_not_merge_compound_with_bare(self)` — *(no docstring)*
  - `test_save_review_accumulates_noise(self)` — *(no docstring)*
  - `test_save_review_preserves_manual_entries(self)` — *(no docstring)*
  - `test_render_review_produces_text(self)` — *(no docstring)*
- `class GazetteerCandidatesTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_high_freq_misc_nodes_become_candidates(self)` — *(no docstring)*
  - `test_existing_gazetteer_name_not_resurfaced(self)` — *(no docstring)*
  - `test_acronym_suggestions_surface_with_corroboration(self)` — *(no docstring)*
  - `test_save_review_carries_acronym_suggestions(self)` — *(no docstring)*
  - `test_gazetteer_candidates_refreshed_not_accumulated(self)` — *(no docstring)*


## `tests/test_sbir.py`

> *(no module docstring — consider adding one)*

**Classes**
- `class IsAIRelatedTest` — *(no docstring)*
  - `test_abstract_machine_learning_is_ai(self)` — *(no docstring)*
  - `test_keywords_only_is_ai(self)` — *(no docstring)*
  - `test_uppercase_ai_acronym_is_ai(self)` — *(no docstring)*
  - `test_ml_acronym_is_ai(self)` — *(no docstring)*
  - `test_data_science_keyword_is_ai(self)` — *(no docstring)*
  - `test_embodied_ai_robotics_is_ai(self)` — *(no docstring)*
  - `test_non_ai_award_is_rejected(self)` — *(no docstring)*
  - `test_lowercase_ai_inside_words_is_not_matched(self)` — *(no docstring)*
- `class AwardToArticleTest` — *(no docstring)*
  - `test_basic_fields(self)` — *(no docstring)*
  - `test_lead_sentence_is_active_voice(self)` — *(no docstring)*
  - `test_url_is_synthesized_when_link_missing(self)` — *(no docstring)*
  - `test_research_institution_yields_partnership_sentence(self)` — *(no docstring)*
  - `test_award_amount_in_text_and_metadata(self)` — *(no docstring)*
  - `test_missing_award_amount_yields_none(self)` — *(no docstring)*
- `class FixtureFilterTest` — *(no docstring)*
  - `test_ai_articles_keeps_only_ai_awards(self)` — *(no docstring)*
- `class FetchAwardsTest` — *(no docstring)*
  - `test_pagination_collects_all_pages(self)` — *(no docstring)*
  - `test_max_records_caps_results(self)` — *(no docstring)*
  - `test_api_error_propagates(self)` — *(no docstring)*
  - `test_get_json_retries_then_raises_on_429(self)` — *(no docstring)*
- `class ScrapeSBIRIntoCorpusTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `test_adds_only_ai_awards(self)` — *(no docstring)*
  - `test_second_run_de_duplicates(self)` — *(no docstring)*
  - `test_skips_gracefully_when_api_unavailable(self)` — *(no docstring)*


## `tests/test_scraper.py`

> *(no module docstring — consider adding one)*

**Classes**
- `class ScraperTest` — *(no docstring)*
  - `test_parse_sample_feed(self)` — *(no docstring)*
  - `test_html_to_text(self)` — *(no docstring)*
  - `test_content_hash_uses_url_and_title_only(self)` — *(no docstring)*
  - `test_extract_text_from_html_empty_returns_fallback(self)` — *(no docstring)*
  - `test_extract_text_from_html_drops_boilerplate(self)` — *(no docstring)*
  - `test_parse_atom_feed(self)` — *(no docstring)*
  - `test_unparseable_input_yields_no_articles(self)` — *(no docstring)*


## `tests/test_server.py`

> *(no module docstring — consider adding one)*

**Classes**
- `class ServerApiTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_graph_endpoint(self)` — *(no docstring)*
  - `test_typed_edge_carries_evidence(self)` — *(no docstring)*
  - `test_edges_carry_strength(self)` — *(no docstring)*
  - `test_search_endpoint(self)` — *(no docstring)*
  - `test_search_matches_aliases(self)` — *(no docstring)*
  - `test_search_finds_documents_by_title(self)` — *(no docstring)*
  - `test_node_and_neighbors(self)` — *(no docstring)*
  - `test_node_not_found(self)` — *(no docstring)*
  - `test_node_documents_endpoint(self)` — *(no docstring)*
  - `test_trends_endpoint(self)` — *(no docstring)*
  - `test_path_endpoint_finds_connection(self)` — *(no docstring)*
  - `test_path_endpoint_unknown_entity_404(self)` — *(no docstring)*
  - `test_types_endpoint(self)` — *(no docstring)*
  - `test_correct_endpoint_writes_corrections(self)` — *(no docstring)*
  - `test_correct_rejects_bad_action(self)` — *(no docstring)*
  - `test_frontend_is_served(self)` — *(no docstring)*
  - `test_capabilities_endpoint(self)` — *(no docstring)*
  - `test_trajectory_endpoint(self)` — *(no docstring)*
  - `test_spikes_endpoint(self)` — *(no docstring)*
  - `test_pulse_endpoint(self)` — *(no docstring)*
  - `test_adjacent_endpoint(self)` — *(no docstring)*
  - `test_adjacent_endpoint_unknown_node_404(self)` — *(no docstring)*
  - `test_hype_endpoint_serves_snapshot_without_a_key(self)` — *(no docstring)*
  - `test_hype_endpoint_no_snapshot_returns_message(self)` — *(no docstring)*
  - `test_hype_refresh_without_key_is_rejected(self)` — *(no docstring)*
  - `test_history_endpoint_empty(self)` — *(no docstring)*
  - `test_history_endpoint_serves_records_with_broken_feeds(self)` — *(no docstring)*
  - `test_briefing_narrative_serves_cached_snapshot(self)` — *(no docstring)*
  - `test_briefing_subfield_filter(self)` — *(no docstring)*


## `tests/test_storage.py`

> *(no module docstring — consider adding one)*

**Classes**
- `class NEROutputLogTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_entities_are_appended(self)` — *(no docstring)*
  - `test_entities_for_filters_by_content_hash(self)` — *(no docstring)*
  - `test_clear_resets_entities_and_ids(self)` — *(no docstring)*
- `class KnowledgeGraphStoreTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_insert_and_query(self)` — *(no docstring)*
  - `test_clear_removes_all_data(self)` — *(no docstring)*
  - `test_node_documents_round_trip(self)` — *(no docstring)*
  - `test_clear_removes_node_documents(self)` — *(no docstring)*


## `tests/test_synthesis.py`

> *(no module docstring — consider adding one)*

**Classes**
- `class SynthesisTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_not_configured_without_key(self)` — *(no docstring)*
  - `test_summarize_raises_without_key(self)` — *(no docstring)*
  - `test_summarize_returns_text_with_mocked_api(self)` — *(no docstring)*
  - `test_api_error_becomes_synthesis_error(self)` — *(no docstring)*
- `class HypeSynthesisTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_hype_raises_without_key(self)` — *(no docstring)*
  - `test_hype_uses_recent_doc_titles_and_snippets(self)` — *(no docstring)*
  - `test_hype_quiet_day_still_produces_a_prompt(self)` — *(no docstring)*
- `class CliTransportTest` — The CLI-first router: when claude_cli is available, prefer it.
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_transport_picks_cli_when_available(self)` — *(no docstring)*
  - `test_summarize_briefing_routes_through_cli(self)` — *(no docstring)*
  - `test_summarize_hype_routes_through_cli(self)` — *(no docstring)*
  - `test_cli_error_becomes_synthesis_error(self)` — *(no docstring)*
- `class TransportPriorityTest` — The router prefers CLI > API > None and reports the chosen one.
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_none_when_neither_transport_present(self)` — *(no docstring)*
  - `test_api_when_only_key_present(self)` — *(no docstring)*
  - `test_cli_preferred_even_when_key_also_present(self)` — *(no docstring)*


## `tests/test_synthesis_cache.py`

> Unit tests for the synthesis snapshot sidecar.

**Classes**
- `class SnapshotShapeTest` — The public API surface of the cache module.
  - `test_make_snapshot_starts_blank(self)` — *(no docstring)*
  - `test_set_section_records_text_and_metadata(self)` — *(no docstring)*
  - `test_set_section_records_failure(self)` — *(no docstring)*
  - `test_set_section_rejects_unknown_name(self)` — *(no docstring)*
- `class StalenessTest` — *(no docstring)*
  - `test_fresh_snapshot_is_not_stale(self)` — *(no docstring)*
  - `test_old_snapshot_is_stale(self)` — *(no docstring)*
  - `test_missing_timestamp_is_treated_as_stale(self)` — *(no docstring)*
  - `test_threshold_is_overridable(self)` — *(no docstring)*
- `class SaveLoadRoundTripTest` — Snapshots round-trip through the JSON file format unchanged.
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_save_then_load_returns_equal_object(self)` — *(no docstring)*
  - `test_load_returns_none_for_missing_file(self)` — *(no docstring)*
  - `test_load_returns_none_for_malformed_file(self)` — *(no docstring)*
  - `test_latest_snapshot_picks_most_recent_by_date(self)` — *(no docstring)*
  - `test_atomic_write_replaces_existing_file(self)` — *(no docstring)*


## `tests/test_trends.py`

> *(no module docstring — consider adding one)*

**Classes**
- `class TrendsTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_document_volume_by_month(self)` — *(no docstring)*
  - `test_new_and_recent_entities_ordering(self)` — *(no docstring)*
  - `test_render_trends_produces_text(self)` — *(no docstring)*
- `class SpikeDetectionTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_long_lived_entity_with_recent_surge_spikes(self)` — *(no docstring)*
  - `test_brand_new_entity_does_not_spike(self)` — *(no docstring)*
  - `test_misc_entities_are_excluded(self)` — *(no docstring)*
  - `test_below_min_recent_does_not_spike(self)` — *(no docstring)*
- `class TrajectoryTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `tearDown(self)` — *(no docstring)*
  - `test_months_returned_in_chronological_order(self)` — *(no docstring)*
  - `test_documents_bucketed_by_published_month(self)` — *(no docstring)*
  - `test_new_entities_bucketed_by_first_seen(self)` — *(no docstring)*


## `tests/test_visualize.py`

> *(no module docstring — consider adding one)*

**Classes**
- `class SelectSubgraphTest` — *(no docstring)*
  - `setUp(self)` — *(no docstring)*
  - `test_top_connected_selection_keeps_the_hub(self)` — *(no docstring)*
  - `test_min_weight_filters_edges(self)` — *(no docstring)*
  - `test_focus_mode_includes_neighbors(self)` — *(no docstring)*
  - `test_focus_no_match_raises(self)` — *(no docstring)*
  - `test_default_view_leads_with_typed_relationship_nodes(self)` — *(no docstring)*
  - `test_focus_ranks_neighbors_by_strength(self)` — *(no docstring)*
  - `test_relations_only_drops_co_occurrence(self)` — *(no docstring)*
  - `test_min_confidence_drops_low_confidence_typed_edges(self)` — *(no docstring)*
  - `test_min_strength_drops_weak_cooccurrence(self)` — *(no docstring)*
  - `test_src_dst_type_filters_typed_edges(self)` — *(no docstring)*
  - `test_type_filter(self)` — *(no docstring)*
  - `test_find_path_returns_shortest_route(self)` — *(no docstring)*
  - `test_find_path_disconnected_returns_empty(self)` — *(no docstring)*
  - `test_find_path_same_node_is_empty(self)` — *(no docstring)*
  - `test_render_writes_self_contained_html(self)` — *(no docstring)*
