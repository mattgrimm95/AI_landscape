# Runtime dependencies.
beautifulsoup4>=4.9
feedparser>=6.0
trafilatura>=2.0
lxml_html_clean>=0.1  # needed by trafilatura's justext dep on lxml >= 5.2
pyvis>=0.3            # static interactive HTML graph export
fastapi>=0.110        # web app backend
uvicorn>=0.27         # ASGI server for the web app
httpx>=0.27           # used by the API test client

# Optional: the spaCy NER backend (select via --ner spacy or
# config.DEFAULT_NER_BACKEND). The pipeline runs fully without it; the
# rule-based backend is the default.
#   pip install "spacy<3.8"   # spaCy 3.8+ requires Python 3.10+
#   python -m spacy download en_core_web_sm

# Optional: pypdf is needed by the J-Books data source (ailandscape/jbooks.py)
# to extract text from DoD Justification Book PDFs. Without it the `jbooks`
# command is a graceful no-op, like a throttled SBIR run.
#   pip install pypdf
