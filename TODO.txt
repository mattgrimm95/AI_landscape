Fix the Jbooks ingest. This tasks only recurs about once a year after the original FY27 and FY26 PDFs are ingested appropriately from the provided links.
[done] create confidence levels for relationship links and allow more leniency for creating links since confidence levels can be applied. -- resolved: every typed-relationship edge now carries a confidence score (rising with the number of independent occurrences) stored in its metadata and exposed in the API; the relation extractor's gap window widened from 55 to 80 chars, raising the typed-relation count from 251 to 408. The detail panel, dossier, and briefing display the confidence next to each typed relationship.
[done] create private and non-pushed email list to send daily news, updates, and POC leads to. -- resolved: ailandscape/emailer.py composes a daily digest (briefing + POC-leads) and sends it via SMTP to addresses in data/email_recipients.txt (a gitignored, private list); `digest` CLI command with --preview; SMTP credentials come from AIL_SMTP_* env vars; a graceful no-op without a recipients file or credentials.
[done] add routine to go over corpus and strengthen attributes and rules. Save new attributes, rules, and changes in a place that accumulates, won't be overrwritten, and can be applied to the knowledge graph database even after reconstruction. -- resolved: ailandscape/review.py + `review` CLI command audit the graph for partial-name duplicate candidates and merge their findings into a version-controlled review.json. The store is never overwritten (each run merges in new findings); its suggested_merges are in the same shape `correct merge` consumes, so they can be applied to the graph and survive reconstruction. Found 63 candidates on the live corpus.
[done] Double check that corpus includes article text and it gets appended to daily. -- verified: the corpus stores full article text (median ~5,600 chars) and daily_scrape.ps1 appends + commits it. Two extraction bugs found and fixed: Python 3.9's urllib does not follow HTTP 308 redirects (this had truncated every OpenAI article to a teaser) and trafilatura precision mode returns nothing on some pages. Added corpus.save, pipeline.backfill_corpus_text, and the `backfill` CLI command, which repaired 60 truncated documents.
[done] J-books as a datastream. As always, target AI related items. Target R&D items primarily. Only use FY27 and FY26 documents for now. For Air-Force: https://www.af.mil/Secretariat-of-the-Air-Force/Financial-Management-SAF-FM/#budget. For Defense-Wide: https://comptroller.war.gov/Budget-Materials/FY2027BudgetJustification/ and https://comptroller.war.gov/Budget-Materials/FY2026BudgetJustification/ -- resolved: ailandscape/jbooks.py crawls a budget-materials index page, fetches PDFs, extracts their text via pypdf (optional dependency), splits each book into program elements, and filters to AI-related R&D items; feeds.py JBOOK_SOURCES carries the FY26/FY27 Defense-Wide + Air Force pages; `jbooks` CLI command and pipeline integration mirror the SBIR pattern (tolerant of source-page or PDF failure).

--- Knowledge-graph navigation & learning roadmap (code review 2026-05-22) ---
[done] * 1. Link every knowledge-graph node to its source articles -- persist each node's source document hashes and show a clickable "mentioned in" article list in the web app detail panel, so navigation reaches the underlying reporting. -- resolved: node_documents table in storage_kg.py, populated by reconcile; /api/node/{id}/documents endpoint; detail panel shows a "Source articles" list of clickable links to the real reporting.
[done] * 2. Attach evidence snippets and provenance to typed relationship edges -- store the cue sentence, source document, and date on each typed edge so every relationship is verifiable. -- resolved: relations.extract_relations now returns an evidence snippet; reconcile stores evidence + source document in the edge metadata; the detail panel shows the snippet under each typed relationship and tapping a typed graph edge surfaces it.
[done] * 3. Fix the default graph view -- lead with typed-relationship edges instead of the degree-ranked co-occurrence hairball, and add a "typed relationships only" toggle. -- resolved: select_subgraph ranks typed-relationship participants first; new relations_only mode (API param, CLI --relations-only, web checkbox) drops co-occurrence entirely.
[done] * 4. In-app dashboard and per-entity dossier pages -- render the statistical overview as a real dashboard and add a one-page profile for any entity. -- resolved: a modal Dashboard (header button) renders the overview with bar charts and clickable entity lists; a per-entity Dossier modal shows typed relationships with evidence, co-occurring entities, and source articles, and links dossier-to-dossier.
[done] * 5. Generated daily/weekly briefing -- a deterministic briefing of new documents, entities, typed relationships, contract awards, and trending topics (CLI + web). -- resolved: ailandscape/briefing.py builds + renders the digest; `briefing` CLI command, /api/briefing endpoint, and a Briefing button opening it in a modal.
[done] * 6. Expand the AI-capability gazetteer -- add a curated AI taxonomy (computer vision, autonomy, drone swarm, LLMs, ISR, electronic warfare, etc.) as concept entities so AI topics are navigable nodes. -- resolved: gazetteer.py now carries ~40 AI-capability surface forms; concept nodes in the live graph rose from 6 to 29.
[done] * 7. Capture SBIR award amounts and structured fields -- carry the dollar figure, phase, and branch into the graph so it shows where defense AI money goes. -- resolved: sbir.award_to_article puts the dollar figure in the lead text (so it lands in the awards_contract edge evidence) and a structured metadata block; corpus documents now carry an optional metadata field; the briefing totals SBIR/STTR funding.
[done] * 8. Normalized co-occurrence strength -- add a hub-corrected edge score (Jaccard/PMI-style) for filtering and edge thickness so weak hub links stop dominating. -- resolved: reconcile stores a Jaccard strength on every co-occurrence edge; the API exposes it, focus-mode neighbour ranking uses it, and graph edge thickness now tracks strength rather than raw weight.
[done] * 9. "How are X and Y connected?" path finding -- shortest-path search between two entities, preferring typed edges. -- resolved: visualize.find_path runs a BFS that expands typed edges first; /api/path endpoint; a Connection panel renders the path as a readable chain of entities and relations.
[done] * 10. Use the article published date (not fetched_at) for node first/last seen -- prerequisite for temporal features. -- resolved: corpus.published_date parses RFC-822 / ISO / bare-year date formats; reconcile dates nodes by publication, falling back to the fetch date only when no published date parses.
[done] * 11. Search across aliases and document text -- so entities are findable by any name form or topic. -- resolved: /api/search now matches canonical names, every alias, and document titles/text, returning entity and article result groups; the search panel shows both.
[done] * 12. Temporal "what's new / trending" view -- mention counts over time and newly appeared entities/edges. -- resolved: ailandscape/trends.py builds document volume by month and newly-appeared / recently-active entity lists; `trends` CLI command, /api/trends endpoint, a Trends modal, and a per-entity activity timeline in the dossier.
[done] * 13. Onboarding / guided entry -- curated starter views and a typed-relationship legend in the web app. -- resolved: a "Start here" panel with curated preset views, a Guide modal explaining the app with a plain-language relationship glossary, shown once automatically on a first visit.
[done] * 14. Auto-merge high-confidence partial-name duplicates -- extend conservative coreference merging to organizations to de-clutter the graph. -- resolved: reconcile._coreference now also folds a one-word organization into the unique multi-word org sharing its first word ("Lockheed" -> "Lockheed Martin"); merged 64 duplicate nodes on the live corpus.
[done] * 15. Optional LLM narrative synthesis -- opt-in module turning a briefing or dossier into an analyst-style summary via the Anthropic API; a graceful no-op without a key. -- resolved: ailandscape/synthesis.py calls the Anthropic Messages API via urllib (no new dependency), reading ANTHROPIC_API_KEY from the environment only; `briefing --narrative` CLI flag and a "Generate analyst narrative" button, both graceful no-ops when no key is set.

[partly done] * Include military focused datasource streams like SAM.gov and SBIR/STTR submissions and winners. Still focus on AI projects and writing only. -- SBIR/STTR awards done: ailandscape/sbir.py pulls awards from the SBIR.gov public API, filters them to AI-related ones, and feeds them into the corpus (CLI: `sbir`). SAM.gov still not done -- it needs an API key.
[done] * create better relationships/links to give more information found within and between articles such as "is_subordinate_to" for sub-organizations and "is_same_organization" for people who work together. -- resolved: ailandscape/relations.py extracts 8 directed, type-constrained typed relations (leads, part_of, located_in, acquires, partners_with, awards_contract, develops, supplies) from cue phrases; "part_of" covers sub-organizations and "leads" links people to the orgs they run.
* Acronym coreference (an acronym and its written-out expansion). [not done with coreference: initials matching is too false-positive prone without article-text context.]
[done] * Add Claude Preview to allow permissions in settings.
[done] * In-GUI correction editing -- resolved: the web app has merge/ignore buttons that POST to /api/correct.
[not needed with preview; skip] Connect to Chrome to view websites.
[done] * In the database summarizer, create more statistics which will help me identify data quality and problem spots.
[done] * Add partial-name / entity coreference resolution so the same entity referred to by different name forms merges into one node -- e.g. "Hegseth" and "Pete Hegseth" (last name to full name).
[done] * Create a knowledge graph GUI/visualization (interactive web app: FastAPI + Cytoscape.js).
[done] * Create a statistical and summarized data based look at the data.
[done] * Are there any other open library upgrades that make sense including spaCy and feedparser.
[done] ** What is the ignore list?
